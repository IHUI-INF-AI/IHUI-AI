# 人工实测与配置清单(2026-09-17 交付)

> 完成方:李春川(需真实账号与密钥)· 预计耗时:约 30 分钟
> 技术侧全部就绪,本清单是项目最后的用户侧收尾。

## 一、.env 关键键(先补这 5-6 个,其余 29 键不影响主链路)

| 键 | 位置 | 用途 | 获取渠道 |
|---|---|---|---|
| `WECHAT_APP_ID` / `WECHAT_APP_SECRET` | apps/api/.env | 微信扫码登录 | 微信开放平台 |
| `SMTP_HOST/PORT/USER/PASS`(或 `RESEND_API_KEY`) | apps/api/.env | 邮件验证码/通知 | 邮箱服务商 |
| `TOKEN6688_API_KEY`(sk- 开头) | apps/ai-service/.env(**两处**:services 配置同步) | 实时语音/TTS e2e 验收 | Token6688 控制台 |
| `USER_TRIAL_DAILY_TOKENS=10000`(可选) | apps/ai-service 部署 env(NSSM AppEnvironmentExtra) | 开启新用户 1 万 token/天 试用额度(默认关闭) | 本次拍板(3-4-A) |

完整 79 空键清单:`tmp/env keys todo.md`。

## 二、桌面端实测(约 15 分钟)

1. 安装最新桌面版(0.1.22+),真实账号登录。
2. **重启应用** → 验证免登录(不重新输密码)。
3. 挂机 15 分钟 → 操作一次 → 验证静默续期无感。
4. 打开 WebView 门户任意域 → 验证 SSO 打通(协议层 2026-09-07 已全链验证,此步为真机 UI 确认)。

## 三、Token6688 e2e(约 5 分钟,补 key 后)

```bash
cd apps/ai-service
.venv/Scripts/python scripts/e2e_token6688.py --cheap
```
预期:生成/取消/声纹克隆全部 PASS(代码侧已无待办,这是唯一验收步)。

## 四、IHUI-Bench 发布(约 2 分钟,一次性 `gh auth login` 后)

```bash
gh auth login
cd outputs/ihui-bench-release
gh repo create ihui-bench --public --source . --push \
  --description "中文编码 Agent 能力基准:35 个自动判定任务,零第三方依赖"
```
发布后在 benchmarks/README 基线榜单追加新仓库链接;后续每周 CI 跑 8 端一致性认证(已接 GitHub Actions 周一 10:00)。

## 五、3-4 额度实验观察(开启后 2 周)

开启 `USER_TRIAL_DAILY_TOKENS=10000` 后,观察:
- 激活率(注册 → 首次 AI 回复)
- 额度触顶用户占比(Redis `user:trial:*` 键数 vs 当日注册数)
- 数据回来后决定正式额度(当前 1 万/天 ×7 天为实验值,可随时调)。
