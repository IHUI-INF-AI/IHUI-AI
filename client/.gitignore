# Dependencies
node_modules/
miniapp/node_modules/

# UniApp 构建产物（由 build:mp-weixin 生成）
miniapp/dist/

# shared packages 打入小程序的单文件 bundle（由 build:shared 生成）
miniapp/src/vendor/shared-api.bundle.js
miniapp/src/vendor/shared-auth.bundle.js
miniapp/src/vendor/shared-services.bundle.js

# shared packages 编译产物（由 build:shared 生成）
packages/*/dist/
.pnp
.pnp.js

# Production builds
dist/
dist-ssr
build/
*.local

# Environment variables - 敏感配置文件
.env
.env.development
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production
.env.production.example   (注: 历史版本忽略，新规则见下方白名单)
.env.docker
# 白名单: .example 模板文件需要提交（生产环境配置迁移）
!.env.production.example
!.env.development.example
!.env.example

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# dev-up 事件日志 (结构化 JSONL, 建议 9: 不提交, 由 size-rotation 自动归档)
dev-up-events.jsonl
dev-up-events.*.jsonl
scripts/dev-up-events.jsonl
../logs/dev-up-events.jsonl
../logs/dev-up-events.*.jsonl

# Temporary output files
*-errors*.txt
*-output*.txt
*-report*.txt
build-errors*.txt
lint*.txt
coverage-report.txt
test-coverage-output.txt
todos-output.txt
temp_*.txt
current-errors.txt
console-messages.json
model-selector-console-logs.json

# Round / cumulative regression leftovers
r7_*
r8_*
r9_*
r10_*
r12_*
r13_*
round7_*
round8_*
round9_*
round10_*
round12_*
round13_*
out*.txt
err*.txt
test_openapi.json
token.txt
hc.txt
uvi.log
import_dbg.log
integration-plan.md
tmp_*.log
tmp_*.py
zhs_smoke_pytest.db

# I18N check reports (keep only FINAL_I18N_CHECK_REPORT.txt)
FINAL_I18N_CHECK_REPORT_*.txt
final-i18n-check-*.txt
!FINAL_I18N_CHECK_REPORT.txt

# TypeScript error logs
ts-errors*.log

# Remaining issues/errors reports
remaining-*.txt
top-issues.txt
final-check-report.txt

# Build error reports (keep for debugging, but ignore duplicates)
build-errors*.txt
!build-errors.txt

# Large file backups (created 2026-06-21 for safe refactoring of large files)
backups/

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# Dependency directories
jspm_packages/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next

# Nuxt.js build / generate output
.nuxt
dist

# Storybook build outputs
.out
.storybook-out

# Temporary folders
tmp/
temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/css-custom-data.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Token 趋势历史数据（不提交）
.token-history/

# E2E 覆盖率趋势历史数据（不提交）
.e2e-coverage-history/
e2e-coverage-report.json
e2e-coverage-trend.md

# Playwright / E2E 生成产物（不提交）
playwright-report/
test-results/
e2e-reports/
.v8-coverage/
e2e-v8-coverage-report/
e2e-v8-coverage-report.json

# 性能监控脚本生成的报告（可本地查看，不提交）
PERFORMANCE_REPORT.md

# Cursor editor global config (should not be in project)
defaultSettings.json
**/defaultSettings.json

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Windows reserved device name file (NUL/CON/PRN/AUX/COM1-9/LPT1-9)
# PowerShell/IDE 终端偶发把 `>nul` 重定向当作文件名写入，Windows 拒绝删除，仅做版本控制忽略
nul
con
prn
aux

# 客服/工单 SQLite 数据（本地持久化，不提交）
backend/data/

# 支付宝加密配置文件（包含密钥，不要提交到Git）
backend-config/.alipay-config.encrypted.json
backend-config/.env.alipay
backend-config/.env.alipay.*
**/.alipay-config.encrypted.json
.agent/claude-skills-collection/SkillsBase

# P20 收尾: 临时日志
frontend-p*.err
vite-p*.err
vite.err

# ============================================================================
# 运行时产物 / 临时文件（P22 清理补充）
# ============================================================================
pw-output/
screenshots/
storybook-static/
audit/
.ruff_cache/
eslint-report.json
eslint-*.txt
e2e_*.txt
test-output.json
temp_*.scss
curl-*.html

# ============================================================================
# 视觉回归测试临时图片（高特异性选择器优化期间生成）
# ============================================================================
temp-*.png
temp-*.jpg
temp-*.jpeg
temp-*.webp

# ============================================================================
# 高特异性选择器分析报告（临时分析产物）
# ============================================================================
hc_*.json
analyze_*.cjs
check_*.cjs
check_*.py
compare-*.cjs
compare-*.ps1
debug-*.cjs
find-*.cjs
find-*.py
gen-*.cjs
verify-*.cjs
verify-*.py
fill-*.cjs
update-*.cjs
update-*.py
i18n-*.cjs
analyze-coverage.cjs
coverage-temp.cjs
