'use client' import * as React from 'react'
import { Loader2, AlertCircle, ChevronRight, FileText } from 'lucide-react' import { cn } from '@/lib/utils'
import { Card, CardHeader, CardTitle, CardContent, Badge, Button } from '@ihui/ui-react'
import { type StudyPlan, PLAN_STATUS_VARIANTS, PLAN_STATUS_LABELS, PLAN_TYPE_LABELS, formatDateDisplay,
} from './types' export function StudyPlanList({ parentPlans, childPlansByParent, selectedPlan, plansLoading, onSelectPlan, onAutoSplit, onEditPlan,
}: { parentPlans: StudyPlan[] childPlansByParent: Map<string, StudyPlan[]> selectedPlan: StudyPlan | null plansLoading: boolean onSelectPlan: (plan: StudyPlan) => void onAutoSplit: (planId: string) => void onEditPlan: (plan: StudyPlan) => void
}) { return ( <Card className="lg:col-span-1"> <CardHeader className="pb-2"> <CardTitle className="text-sm font-medium"> {selectedPlan ? '计划列表' : '计划列表'} {parentPlans.length > 0 && ( <span className="ml-2 text-xs text-muted-foreground">({parentPlans.length})</span> )} </CardTitle> </CardHeader> <CardContent className="p-0"> {plansLoading ? ( <div className="flex items-center justify-center text-muted-foreground"> <Loader2 className="mr-2 h-5 w-5 animate-spin" /> 加载中... </div> ) : parentPlans.length === 0 ? ( <div className="flex flex-col items-center justify-center text-muted-foreground"> <AlertCircle className="mb-2 h-8 w-8" /> <p className="text-sm">暂无学习计划</p> </div> ) : ( <div className="space-y-1"> {parentPlans.map((plan) => { const children = childPlansByParent.get(plan.id) ?? [] const isUnsplitted = plan.planType === 'monthly' && children.length === 0 return ( <div key={plan.id}> <button type="button" className={cn( 'w-full px-4 py-3 text-left transition-colors hover:bg-accent/50', selectedPlan?.id === plan.id && 'bg-accent', )} onClick={() => onSelectPlan(plan)} > <div className="flex items-start justify-between gap-2"> <div className="min-w-0 flex-1"> <div className="flex items-center gap-2"> <p className={cn( 'text-sm font-medium truncate', plan.status === 'completed' && 'line-through text-muted-foreground', )} > {plan.title} </p> </div> <div className="mt-1 flex flex-wrap items-center gap-1.5 text-xs text-muted-foreground"> <Badge className={cn('px-1.5', PLAN_STATUS_VARIANTS[plan.status])}> {PLAN_STATUS_LABELS[plan.status]} </Badge> <Badge variant="outline">{PLAN_TYPE_LABELS[plan.planType]}</Badge> {isUnsplitted && ( <Badge variant="outline" className="border-amber-300 text-amber-600"> 未拆解 </Badge> )} </div> <p className="mt-1 text-xs text-muted-foreground"> {formatDateDisplay(plan.startDate)} ~ {formatDateDisplay(plan.endDate)} </p> </div> <ChevronRight className={cn( 'h-4 w-4 shrink-0 text-muted-foreground transition-transform', selectedPlan?.id === plan.id && 'rotate-90', )} /> </div> {/* Action buttons in card */} <div className="mt-2 flex items-center gap-2"> {isUnsplitted && ( <Button size="sm" variant="outline" className="h-6 text-xs" onClick={(e) => { e.stopPropagation() onAutoSplit(plan.id) }} > <FileText className="mr-1 h-3 w-3" /> 拆解 </Button> )} <Button size="sm" variant="ghost" className="h-6 text-xs" onClick={(e) => { e.stopPropagation() onEditPlan(plan) }} > 编辑 </Button> </div> </button> {/* Child plans rendered as sub-items */} {children.length > 0 && ( <div className="border-t bg-muted/20"> {children.map((child) => ( <button key={child.id} type="button" className={cn( 'w-full flex items-center gap-2 px-6 py-2 text-left text-xs transition-colors hover:bg-accent/50', selectedPlan?.id === child.id && 'bg-accent', )} onClick={() => onSelectPlan(child)} > <ChevronRight className="h-3 w-3 text-muted-foreground" /> <span className="truncate font-medium">{child.title}</span> <Badge className={cn('px-1 text-[10px]', PLAN_STATUS_VARIANTS[child.status])} > {PLAN_STATUS_LABELS[child.status]} </Badge> <span className="shrink-0 text-muted-foreground"> {formatDateDisplay(child.startDate)}~{formatDateDisplay(child.endDate)} </span> </button> ))} </div> )} </div> ) })} </div> )} </CardContent> </Card> )
}
