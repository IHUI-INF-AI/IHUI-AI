'use client' import { useTranslations } from 'next-intl'
import type { Dispatch, SetStateAction } from 'react'
import type { DesignComment } from '@ihui/shared/design/element'
import { CSS_GROUPS } from './css-config'
import type { SelectedElement, RightPanelTab, TranslationFn } from './design-types'
import { CssGroupSection } from './CssInspector' interface InspectorPanelProps { selected: SelectedElement | null onResetStyles: () => void styleEdits: Record<string, string> onStyleChange: (prop: string, value: string) => void commentOpen: boolean setCommentOpen: Dispatch<SetStateAction<boolean>> commentText: string onCommentTextChange: (v: string) => void onSubmitComment: () => void commentPosting: boolean commentMsg: string currentPreviewId: string commentsLoading: boolean commentsError: string comments: DesignComment[] dateFormatter: Intl.DateTimeFormat rightTab: RightPanelTab onTabChange: (tab: RightPanelTab) => void
} export function InspectorPanel({ selected, onResetStyles, styleEdits, onStyleChange, commentOpen, setCommentOpen, commentText, onCommentTextChange, onSubmitComment, commentPosting, commentMsg, currentPreviewId, commentsLoading, commentsError, comments, dateFormatter, rightTab, onTabChange,
}: InspectorPanelProps) { const t = useTranslations() return ( <section style={{ flex: '0 0 280px', display: 'flex', flexDirection: 'column', gap: 8, overflow: 'hidden', }} > <div style={{ display: 'flex', gap: 4 }}> <button type="button" onClick={() => onTabChange('css')} style={{ flex: 1, padding: '6px 8px', fontSize: 12, fontWeight: rightTab === 'css' ? 600 : 400, border: '1px solid var(--border)', borderBottom: rightTab === 'css' ? 'none' : '1px solid var(--border)', borderRadius: '6px 6px 0 0', background: rightTab === 'css' ? 'var(--card)' : 'transparent', cursor: 'pointer', }} > {t('design.cssPanel')} </button> <button type="button" onClick={() => onTabChange('comments')} style={{ flex: 1, padding: '6px 8px', fontSize: 12, fontWeight: rightTab === 'comments' ? 600 : 400, border: '1px solid var(--border)', borderBottom: rightTab === 'comments' ? 'none' : '1px solid var(--border)', borderRadius: '6px 6px 0 0', background: rightTab === 'comments' ? 'var(--card)' : 'transparent', cursor: 'pointer', }} > {t('design.comments.title')}({comments.length}) </button> </div> {rightTab === 'css' ? ( <div style={{ display: 'flex', flexDirection: 'column', gap: 8, overflow: 'auto', flex: 1, padding: 8, border: '1px solid var(--border)', borderTop: 'none', borderRadius: '0 0 6px 6px', }} > {selected ? ( <> <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 8, }} > <span style={{ fontSize: 12, color: 'var(--muted)' }}> {selected.tagName} {selected.elementId ? `#${selected.elementId}` : ''} </span> <button type="button" onClick={onResetStyles} style={{ fontSize: 11, padding: '2px 8px', border: '1px solid var(--border)', borderRadius: 4, background: 'transparent', cursor: 'pointer', color: 'var(--muted)', }} > {t('design.css.reset')} </button> </div> {CSS_GROUPS.map((group) => ( <CssGroupSection key={group.id} group={group} values={styleEdits} onChange={onStyleChange} t={t as TranslationFn} /> ))} <button type="button" onClick={() => setCommentOpen((v) => !v)} style={{ marginTop: 8 }} > {t('design.commentToChat')} </button> {commentOpen && ( <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}> <textarea value={commentText} onChange={(e) => onCommentTextChange(e.target.value)} placeholder={t('design.commentPlaceholder')} style={{ resize: 'none', fontSize: 12, minHeight: 60 }} /> <div style={{ display: 'flex', gap: 8 }}> <button type="button" onClick={onSubmitComment} disabled={commentPosting}> {commentPosting ? t('common.loading') : t('design.comments.send')} </button> <button type="button" onClick={() => setCommentOpen(false)}> {t('common.cancel')} </button> </div> </div> )} </> ) : ( <span style={{ fontSize: 12, color: 'var(--muted)' }}> {t('design.clickElementHint')} </span> )} {commentMsg && ( <p style={{ margin: 0, fontSize: 12, color: 'var(--muted)' }}>{commentMsg}</p> )} </div> ) : ( <div style={{ display: 'flex', flexDirection: 'column', gap: 8, overflow: 'auto', flex: 1, padding: 8, border: '1px solid var(--border)', borderTop: 'none', borderRadius: '0 0 6px 6px', }} > {!currentPreviewId && ( <span style={{ fontSize: 12, color: 'var(--muted)' }}> {t('design.comments.empty')} </span> )} {currentPreviewId && commentsLoading && ( <span style={{ fontSize: 12, color: 'var(--muted)' }}>{t('common.loading')}</span> )} {currentPreviewId && commentsError && ( <span style={{ fontSize: 12, color: 'var(--danger, #dc2626)' }}> {t('design.comments.loadingFailed')}: {commentsError} </span> )} {currentPreviewId && !commentsLoading && !commentsError && comments.length === 0 && ( <span style={{ fontSize: 12, color: 'var(--muted)' }}> {t('design.comments.noComments')} </span> )} {comments.map((c) => ( <div key={c.id} style={{ padding: '6px 8px', border: '1px solid var(--border)', borderRadius: 6, background: 'var(--background, transparent)', display: 'flex', flexDirection: 'column', gap: 4, }} > <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', gap: 8, }} > <span style={{ fontSize: 11, fontWeight: 600, color: 'var(--text, inherit)' }}> {c.userName || `User ${c.userId}`} </span> <span style={{ fontSize: 10, color: 'var(--muted)' }}> {dateFormatter.format(new Date(c.createdAt))} </span> </div> {c.elementId && ( <span style={{ fontSize: 10, color: 'var(--muted)', fontFamily: 'monospace' }}> #{c.elementId} </span> )} <span style={{ fontSize: 12 }}>{c.content}</span> </div> ))} </div> )} </section> )
}
