'use client' import { useTranslations } from 'next-intl'
import { DESIGN_TEMPLATES } from '@/lib/design/design-templates'
import { TEMPLATE_CATEGORY_KEY } from './css-config' interface TemplateDialogProps { open: boolean onClose: () => void onApplyTemplate: (templateHtml: string) => void
} export function TemplateDialog({ open, onClose, onApplyTemplate }: TemplateDialogProps) { const t = useTranslations() if (!open) return null return ( // eslint-disable-next-line jsx-a11y/click-events-have-key-events, jsx-a11y/no-static-element-interactions -- 模态遮罩点击外部关闭;键盘用户通过关闭按钮提供等价交互 <div onClick={onClose} style={{ position: 'fixed', inset: 0, zIndex: 50, display: 'flex', alignItems: 'center', justifyContent: 'center', background: 'rgba(0,0,0,0.4)', }} > {/* eslint-disable-next-line jsx-a11y/click-events-have-key-events, jsx-a11y/no-static-element-interactions -- 阻止点击冒泡到遮罩层(避免误关闭);键盘用户通过关闭按钮关闭 */} <div onClick={(e) => e.stopPropagation()} style={{ width: 'min(720px, 90vw)', maxHeight: '80vh', overflow: 'auto', background: 'var(--card)', border: '1px solid var(--border)', borderRadius: 8, padding: 16, display: 'flex', flexDirection: 'column', gap: 12, }} > <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}> <h3 style={{ margin: 0, fontSize: 16, fontWeight: 600 }}> {t('design.templates.title')} </h3> <button type="button" onClick={onClose} style={{ border: 'none', background: 'transparent', cursor: 'pointer', fontSize: 18, color: 'var(--muted)', lineHeight: 1, }} aria-label={t('common.close')} > × </button> </div> <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(160px, 1fr))', gap: 8, }} > {DESIGN_TEMPLATES.map((tpl) => ( <button key={tpl.id} type="button" onClick={() => onApplyTemplate(tpl.html)} style={{ display: 'flex', flexDirection: 'column', gap: 4, padding: 10, textAlign: 'left', border: '1px solid var(--border)', borderRadius: 6, background: 'var(--card)', cursor: 'pointer', color: 'var(--text, inherit)', }} onMouseEnter={(e) => { e.currentTarget.style.background = 'var(--accent-soft, rgba(0,0,0,0.04))' e.currentTarget.style.borderColor = 'var(--accent, hsl(142 71% 45%))' }} onMouseLeave={(e) => { e.currentTarget.style.background = 'var(--card)' e.currentTarget.style.borderColor = 'var(--border)' }} > <span style={{ fontSize: 12, fontWeight: 600 }}>{t(tpl.nameKey)}</span> <span style={{ fontSize: 11, color: 'var(--muted)' }}>{t(tpl.descriptionKey)}</span> <span style={{ fontSize: 10, color: 'var(--muted)', marginTop: 2 }}> {t(TEMPLATE_CATEGORY_KEY[tpl.category] ?? 'design.templates.categories.unknown')} </span> </button> ))} </div> </div> </div> )
}
