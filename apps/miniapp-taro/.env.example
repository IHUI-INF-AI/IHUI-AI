# ============================================================================
# IHUI-AI 小程序 (Taro 4) 环境变量示例
# ----------------------------------------------------------------------------
# 使用方法: cp .env.example .env.local, 然后替换为实际值
# Taro CLI 自动加载 .env / .env.local / .env.<mode> 文件,并将 TARO_APP_ 前缀
# 变量在编译期内联(DefinePlugin),无需在 config/index.ts 额外配置。
# 注意:修改 env 后必须重新执行 taro build / taro dev 才会生效。
# ============================================================================

# --- 后端 API 地址 ---
# 本地开发默认回退 http://localhost:8802/api,可不配置。
# 真机预览 / 生产发布必须配置为线上 HTTPS 地址,否则小程序真机无法连接本地服务。
# 以下为示例占位值,复制到 .env.local 后请替换为实际线上地址。
# 要求:
#   1. 需在微信公众平台 / 支付宝开放平台把该域名加入 request 合法域名(必须 https)。
#   2. 建议带 /api 后缀(与 BASE_URL 约定一致,调用方会按需剥离 /api 拼接 SSO 等端点)。
TARO_APP_API_BASE=https://api.example.com/api
