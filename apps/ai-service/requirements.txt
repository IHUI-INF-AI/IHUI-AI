# AI Service Python 依赖(本文件由 pyproject.toml 派生,仅列出运行时关键依赖;
# 完整依赖图见 uv.lock。新增依赖必须同时更新 pyproject.toml 与本文件)。

# --- Web 框架 ---
fastapi>=0.141.0
uvicorn[standard]>=0.52.0
pydantic>=2.13.0
pydantic-settings>=2.14.0

# --- Socket.IO 兼容协议层(2026-07-20 新增)---
# 兼容历史 coze_zhs_py 客户端通过 Socket.IO 协议连接新 ai-service。
# ASGI 集成:socketio.ASGIApp(sio, other_asgi_app=fastapi_app)
python-socketio>=5.16.0
python-engineio>=4.13.0

# --- 数据存储 ---
asyncpg>=0.31.0
cryptography>=50.0.0
redis[hiredis]>=8.1.0

# --- 任务调度(对标 Codex Automations,2026-07-24 立)---
# APScheduler AsyncIOScheduler:cron/date/interval 三种 trigger + 后台 worker
# croniter:schedule_task 工具计算 cron 表达式的 next_run_at
# APScheduler 保持 3.x(4.x 是完全重写,有重大 breaking change)
apscheduler>=3.11.0
croniter>=6.2.0

# --- AI / LLM ---
# P3 深度层升级(2026-07-23 立):PostgresSaver checkpointer + interrupt() HITL + 5 模式 streaming + Time Travel
langgraph>=1.2.0
langgraph-checkpoint-postgres>=3.1.0
psycopg[binary]>=3.3.0
langchain>=1.3.0
langchain-core>=1.5.0
langchain-openai>=1.4.0
langchain-anthropic>=1.5.0
litellm>=1.95.0
# MCP 依赖已删除(2026-08-02):代码库未使用 mcp 库,mcp_server.py 是完全自定义实现

# --- 网络 / 序列化 / 重试 ---
httpx>=0.28.1
orjson>=3.11.0
tenacity>=9.1.0
structlog>=26.1.0
tiktoken>=0.13.0
numpy>=2.4.0
# P1-3:子进程资源监控(psutil 可选,未安装时降级为仅超时控制)
psutil>=7.2.0

# --- 鉴权 / 可观测性 ---
pyjwt>=2.13.0
prometheus-fastapi-instrumentator>=8.1.0
opentelemetry-sdk>=1.44.0
opentelemetry-exporter-otlp>=1.44.0
opentelemetry-instrumentation-fastapi>=0.65b0

# --- 测试 ---
fakeredis>=2.37.0
