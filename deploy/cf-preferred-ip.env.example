# cf-preferred-ip 配置模板——复制为同目录 cf-preferred-ip.env 后填写
# 本文件含密钥, 已加入 .gitignore, 切勿入库。
#
# 1) Cloudflare API 令牌(需 Zone.DNS 编辑):
#    登录 dash.cloudflare.com → 我的个人资料 → API 令牌 → 创建令牌 →
#    模板选「编辑区域 DNS」, 区域授权选 aizhs.top。
CLOUDFLARE_API_TOKEN=
# 2) aizhs.top 的 zone_id:
#    curl "https://api.cloudflare.com/client/v4/zones?name=aizhs.top" -H "Authorization: Bearer $TOKEN"
#    取返回 JSON 的 result[0].id。
CF_ZONE_ID=
# 3) 要优选的主机记录(默认 img)
CF_RECORD=img
# 4) XIU2/CloudflareSpeedTest 可执行文件路径(开源, GitHub 免费下载)
CST_BIN=D:\tools\CloudflareST.exe
# 5) 可选: 写回 TTL(秒) 与 proxied 布尔(默认照旧橙云开)
CF_TTL=300
# CF_PROXIED=true
# 6) 可选: 附加测速参数(默认并发200/免下载测速)
# CST_ARGS=-tp 200 -dd