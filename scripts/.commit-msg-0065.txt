fix(api+db): admin /member/users 完整支持 level 字段(level 越界返回 400)

修复 BUG: apps/api/src/routes/admin-missing-routes.ts 中
GET /api/admin/member/users 的 level 查询参数,以及
PATCH /api/admin/member/users/:id 的 level body 字段,
此前被静默忽略(无 level 过滤,无 level 更新)。

同时修复 level/status 查询参数解析失败时(例如 level=5)
原本静默忽略返回 200,改为返回 400,符合 API 一致性。

- packages/database/drizzle/0065_users_level.sql:
  新增 migration(幂等 ADD COLUMN IF NOT EXISTS + 索引)
- packages/database/drizzle/meta/_journal.json: 注册 0065(idx 66)
- packages/database/src/schema/users.ts: users 表加 level 字段定义
- apps/api/src/db/member-queries.ts: findUsersByDepartment 补全 level 字段
- apps/api/src/routes/admin-missing-routes.ts:
  - GET /api/admin/member/users 支持 level 查询过滤(0-3)
  - PATCH /api/admin/member/users/:id 支持更新 level
  - POST /api/admin/member/users 支持创建时指定 level
  - GET 中 status/level 解析失败返回 400
- apps/api/tests/admin-missing-routes.real.test.ts:
  12 个真实 DB 集成测试覆盖 level 字段 CRUD + 边界
- scripts/apply-0065.mjs: migration 0065 应用脚本

level 字段语义:0=普通 1=白银 2=黄金 3=钻石(用户会员显示等级)
与 is_vip 区分:is_vip 是会员身份(-1=游客 0=普通 1=VIP 2=操盘手)

验证:
- pnpm --filter @ihui/api typecheck 0 错误
- pnpm --filter @ihui/api lint 0 错误
- pnpm --filter @ihui/database typecheck 0 错误
- pnpm --filter @ihui/api vitest run: 193 文件 / 2989 测试 0 失败
- pnpm --filter @ihui/api vitest run --config vitest.real.config.ts:
  37 文件 / 510 测试 0 失败(含本文件 12/12 通过)
- pnpm --filter @ihui/database --filter @ihui/auth --filter @ihui/config
  --filter @ihui/types build 全绿
